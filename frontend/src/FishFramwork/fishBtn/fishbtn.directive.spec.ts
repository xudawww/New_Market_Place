import { FishbtnDirective } from './fishbtn.directive';

describe('FishbtnDirective', () => {
  it('should create an instance', () => {
    const directive = new FishbtnDirective();
    expect(directive).toBeTruthy();
  });
});
