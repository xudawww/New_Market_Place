import { FishcardHeaderDirective } from './fishcard-header.directive';

describe('FishcardHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FishcardHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
